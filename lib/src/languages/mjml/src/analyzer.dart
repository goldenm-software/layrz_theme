part of '../mjml.dart';

class MjmlAnalizer extends AbstractAnalyzer {
  @override
  Future<AnalysisResult> analyze(Code code) async {
    // TODO Develop an analyzer for MJML
    return const AnalysisResult(issues: []);
  }
}
